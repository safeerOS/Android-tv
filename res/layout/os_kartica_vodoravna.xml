<?xml version="1.0" encoding="utf-8"?>
<!-- Aplikacija televizorja: ikona levo, ime desno. Vodoravna kartica je nizja in z dveh metrov
     bolj berljiva kot ikona z imenom pod njo. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="204dp"
    android:layout_height="64dp"
    android:layout_marginEnd="16dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@drawable/os_kartica"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:focusable="true"
    android:focusableInTouchMode="false"
    android:clickable="true">
    <ImageView
        android:id="@+id/ikona"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:scaleType="fitCenter"
        android:contentDescription="@null"/>
    <TextView
        android:id="@+id/ime"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:maxLines="2"
        android:ellipsize="end"
        android:textColor="@color/os_besedilo"
        android:textSize="14sp"
        android:fontFamily="sans-serif-medium"/>
</LinearLayout>
